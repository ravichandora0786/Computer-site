export const selectScreenLoader = (state) => state.common.fullScreenLoader;
export const selectUser = (state) => state.common.user;
export const selectAccessToken = (state) => state.common.accessToken;
export const selectPermissions = (state) => state.common.rolePermissionsMap;
